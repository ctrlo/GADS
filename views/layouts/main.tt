<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">

    <title>[% page %] | Linkspace</title>

    <meta name="author" content="Digitpaint">

    <meta name="description" content="">
    <meta name="author" content="">
    <meta name="generator" content="Roger" />
    <meta name="viewport" content="width=device-width,minimum-scale=1,initial-scale=1">

    <!-- location dependent stylesheets for CDN om prod. check if still needed-->
    [% IF hostlocal %]

    [% ELSE %]

    [% END %]
    <link rel="stylesheet" href="[% url.css %]/general.css">
    <link rel="stylesheet" href="[% url.css %]/external.css">
  </head>

  <body class="[% body_class %]" data-csrf="[%- csrf_token -%]">
    <div class="[% container_class %]">
      [%
        IF page == "login" OR page == "register" OR page == "reset" OR page == "invalidsite" OR page_as_mech;
          PROCESS layouts/content_without_menu.tt;
        ELSE;
          PROCESS layouts/content_with_menu.tt;
        END;
      %]
    </div>
    <script type="text/javascript" src="[% url.js %]/site.js"></script>
  </body>
</html>
