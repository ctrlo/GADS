export const goodUser = "test@example.com";
export const goodPassword = "xyz123";

export const baseUser = "basic@example.com";
export const basePassword = "xyz123";

export type instanceMode = "layout" | "permissions" | "topics" | "edit" | "data" | "report" | undefined;

export type tablePermissions = "Bulk import records" | "Purge deleted records" | "Download records" | "Delete records" | "Bulk update records" | "Bulk delete records" | "Manage linked records" | "Manage child records" | "Manage views" | "Manage group views" | "Select extra view limits" | "Manage fields" | "Send messages";