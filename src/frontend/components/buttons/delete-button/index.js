import { initializeComponent } from "../../../js/lib/component";
import DeleteButtonComponent from "./lib/component";

export default (scope) =>
  initializeComponent(scope, "btn-js-delete", DeleteButtonComponent);
