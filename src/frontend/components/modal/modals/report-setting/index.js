import { initializeComponent } from 'component'
import ReportSettingComponent from './lib/component'

export default (scope) => initializeComponent(scope, '.modal--edit-setting', ReportSettingComponent)
