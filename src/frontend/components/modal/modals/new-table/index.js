import { initializeComponent } from 'component'
import AddTableModalComponent from './lib/component'

export default (scope) => initializeComponent(scope, '.modal--new-table', AddTableModalComponent)
